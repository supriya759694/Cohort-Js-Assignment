# 🃏 Cards Collection Project  
### A Dynamic Profile Cards UI using JavaScript DOM + Loops  

This project generates a collection of profile cards using **pure JavaScript**, **DOM manipulation**, and **loops**.  
Instead of writing multiple HTML card blocks manually, all cards are created **dynamically** from a data array.

This makes the project scalable, fast, and perfect for beginners learning JavaScript.

---

## 🚀 Features

### ✔ Dynamic Card Generation  
- Data stored in an array of objects  
- Cards created using `forEach()` loop  
- DOM methods like `createElement`, `appendChild`, and `innerHTML`  

### ✔ Search Functionality  
- Real-time search  
- Filters cards by name  
- Uses DOM events + simple string matching  

### ✔ Sorting (A–Z / Z–A)  
- Sorts the card list alphabetically  
- Uses `localeCompare()` for clean sorting  
- Renders sorted data dynamically  

### ✔ Light / Dark Theme Toggle  
- Simple theme switch button  
- Uses `classList.toggle()`  
- Smooth UI transition  

### ✔ Hover Animation  
- Clean hover effect  
- Slight scale & elevation  
- Modern UI feel  

---

## 🛠️ Technologies Used

- **HTML** – Base layout  
- **CSS** – Card styling, animations, theme  
- **JavaScript **  
  - DOM manipulation  
  - Loops (`forEach`)  
  - Arrays & Objects  
  - Events (input, change, click)  

---


